[project]
name = "dbt-snowflake"
version = "0.1.0"
description = "DBT Snowflake Project"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dbt-core>=1.12.5",
    "dbt-snowflake>=1.12.1",
]

[tool.uv]
package = false
