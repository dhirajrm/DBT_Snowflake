snapshots:
  - name: dim_bookings
    relation: ref('bookings') # source('my_source', 'my_table') or ref('my_model')
    config:
      database: AIRBNB
      schema: gold
      strategy: timestamp
      updated_at: created_at
      unique_key: booking_id
      dbt_valid_to_current: "to_date('9999-12-31')"
